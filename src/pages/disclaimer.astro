---
import LegalPageContent from "../components/ui/LegalPageContent/LegalPageContent.astro";
import { studio } from "../config/studio";
import { disclaimerPageContent } from "../config/legal/disclaimer";
import BaseLayout from "../layouts/BaseLayout.astro";

const { seo, title, intro, sections, reviewDate } = disclaimerPageContent;
---

<BaseLayout
	title={`${seo.title} | ${studio.name}`}
	description={seo.description}
	canonicalPath={seo.canonicalPath}
	solidNav={true}
>
	<LegalPageContent
		title={title}
		intro={intro}
		sections={sections}
		reviewDate={reviewDate}
	/>
</BaseLayout>
